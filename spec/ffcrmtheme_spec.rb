require "spec_helper"

describe Ffcrmtheme do
  it "has a version number" do
    expect(Ffcrmtheme::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
